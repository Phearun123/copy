
spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5433/copy
    username: postgres
    password: 12345
  jpa:
    hibernate:
      ddl-auto: update
server:
  port: 1000
